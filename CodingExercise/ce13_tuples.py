# Create a color_codes variable and assign a tuple to it.
# The tuple should contain three tuples as items.
# Each of the three tuples should contain three items.
# The items can be any type.
# 1. The structure should be similar to this: ((), (), ())

color_codes = (('red', 5.5, 3), ('orange', 4.5, 2), ('yellow', 9.5, 6))

