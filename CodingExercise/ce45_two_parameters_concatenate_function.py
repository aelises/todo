# Implements a function that takes two strings as parameters, concatenates them and returns the result.

def two_strings(s1, s2):
    # Concatenate the two provided strings
    return s1 + s2


print(two_strings("Alan", "Nino"))
